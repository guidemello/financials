/*
pmt = function(pv, n, i) {
    pmt = pv/((1-(1+i)**(-n))/i)
    console.log(`pmt = ${Math.round(pmt*100)/100}`)
}
pmt(10000, 12, 0.01)

pv = function(pmt, n, i) {
    pv = pmt * ((1-(1+i)**(-n))/i)
    console.log(`pv = ${Math.round(pv*100)/100}`)
}
pv(100, 12, 0.01)
*/

